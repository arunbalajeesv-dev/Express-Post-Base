services:
  - type: web
    name: express-post-api
    env: node
    plan: free
    buildCommand: npm install -g pnpm && pnpm install --frozen-lockfile && pnpm --filter @workspace/sales-app build && pnpm --filter @workspace/api-server build
    startCommand: node --enable-source-maps --dns-result-order=ipv4first ./artifacts/api-server/dist/index.mjs
    envVars:
      - key: NODE_ENV
        value: production
      - key: DATABASE_URL
        sync: false
      - key: JWT_SECRET
        sync: false
